window.addEventListener('load', () => {
    /* -------------------------------- listeners ------------------------------- */
    signupButton.addEventListener('click', (e) => userHandler.signUp(e))
})